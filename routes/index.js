const app_route   = require('./app');
const user_route  = require('./user');
const admin_route = require('./admin');
const agent_route = require('./agent');
const auth_route = require('./auth');

module.exports = {
    app_route,
    user_route,
    admin_route,
    agent_route,
    auth_route
};